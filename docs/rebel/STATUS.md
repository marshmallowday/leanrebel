# ReBeL status — M06 in progress; M05 accepted

## Resume point

Use the actual remote HEAD of `rebel/m06-depth-budget-integration-20260924`.
Its base is0309f7c022d3911bb3eae6a64790b76a1b0dcc91, preserving the repaired
first-exit source487aaedd795d5d3a8b41a608c6a4e13d1967cb1c and review records.
Main remains6a6cbdaa8b4fb43b35a9d3e555a7c1a614130098 and is not a write target.
Read exact-SHA CI before continuing; never promote a pending result to success.

## First-exit source accepted through all inspected workflows

Source487aaedd passed M06 target35941683756/job107450728931 and inventory
35941683774. The previously pending ReBeL35941683758/job107450798403 and full
CI35941683736/job107450816152 are now both completed successfully. The former
finished the complete compiler/lint/transitive-axiom audit and cleanliness;
the latter finished Phase1/2/3 architecture checks and full-library lint.
This supersedes the pending snapshots in M06-first-exit-validation.md.
Source01245b31's earlier recovery evidence remains accepted and separate.
Do not repeat the support repair, first-exit construction or its controls.

## Current slice: integrate the existing finite depth-budget backend

An existing divergent branch rebel/m06-depth-budget-20260924 at2c9bfd52 contains
PBSInformationDepthBudget and its hidden-type controls, but neither file was
on the accepted first-exit branch or in its build/audit consumer lists. The
integration reuses those two blobs and adds them to the analytic root, M06
targets and supplemental normal/slow lint plus transitive axiom audit. It does
not restore the older branch's first-hit files or its obsolete STATUS.
The original branch and the accepted first-exit branches remain untouched.

The actual noisy depth solver gets a computed positive finite parent iteration
count for every feasible target. Fixed prediction error and positive child loss
remain in the bound. A positive tolerance allocation proves feasibility, and
the actual joint law's positive mass floor supplies the conditional child target.
No Nash certificate, arbitrary probability floor or zero-noise reduction is used.
This new slice remains UNACCEPTED until its own target-SHA validation succeeds.
See M06-depth-budget-integration.md for provenance, declarations and scope.

## Remaining original obligations

Install the budgeted depth backend into the factual-child public splice and
zero-own-reach completion, then connect arbitrary-depth structural recursion;
the deepest child currently still uses full-root CFR. Derive source-level
model-value drift and useful first-exit/support-defect bounds. Discharge
CarriedResolveStepBounds rather than assuming it. Neither the computed real
sampling charge nor the iteration allocator is a learned-network accuracy or
executable numerical-refinement theorem. Retain finite outer T, positive
numerical/child errors, zero-reach completion and the printed/corrected Theorem3
distinction. SEARCH-FRONTIER, SEARCH-CFRD, SEARCH-ERROR and SAFE-THEOREM3 remain
pending; earlier source qualifications, ledgers and M05 acceptance are unchanged.
