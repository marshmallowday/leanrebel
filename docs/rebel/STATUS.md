# ReBeL status — M06 in progress; M05 accepted

## Active query-sampling continuation

Continue on `rebel/m06-query-sampling-20260921`, based on integration
`8ec014ee7b5a2fe8d709e50512d3c58364dc0b6e`. Read the actual remote HEAD
and its exact-source CI before further edits. Main remains accepted M05 at
`6a6cbdaa8b4fb43b35a9d3e555a7c1a614130098`; no main integration or history
rewrite has been performed.

`M06-query-sampling-restart.md` records the inspected integration CI:
run `35571011975`, job `106242460247` passed the full 4222-job build,
compiler-resolved inventory and Phase 1/2/3 architecture checks, then failed
full-library lint on one underscored definition name. This checkpoint renames
that definition and all source/example uses to `pbsInformationCFRSampleFrom`.
No audit is disabled and no theorem is weakened. The repaired source needs its
own validation; parent success does not validate this commit.

## Existing mathematical construction

The integrated source combines actual information-set CFR iteration families,
private uniform retained-index laws, parent public-cut splices and independent
two-player sampling. `PBSSupportedSampling` additionally separates complete-history
root fibers and proves the one-player law at every positive-mass model root and
any supported reweighting. These are already implemented, not new remaining work.
The model PBS need not equal the actual unknown opponent's posterior.

The live hidden-type controls retain the two-iteration child solve, supported
single-root and point-mass reweighting, zero-fuel boundary, zero-mass cancellation
counterexample, and product-versus-diagonal index-law distinction. No per-iterate
Nash or per-action-redraw equivalence is asserted.

Previous integration provenance is in `M06-sampling-validation-restart.md` and
the STATUS at commit `8ec014ee7b5a2fe8d709e50512d3c58364dc0b6e`.
The older supported-source STATUS remains in
`M06-status-before-independent-sampling.md`.

## Next original M06 obligations

Connect the actual child iteration families to the parent's canonical
counterfactual/type-conditioned queries and zero-own-reach completion. Then prove
independently re-solved recursive carried-PBS play against a fixed unknown
opponent. One-PBS sampling laws and one-cut security are not this final theorem.

Keep positive prediction and child errors, outer finite T, posterior-dependent
child budgets, and printed/corrected Theorem 3 distinct. No positive-fuel guarantee
outside model support, lossless arbitrary Nash replacement, numerical refinement
or full-framework acceptance follows. `SEARCH-FRONTIER`, `SEARCH-CFRD`,
`SEARCH-ERROR` and `SAFE-THEOREM3` remain pending in the unchanged hashed coverage.
