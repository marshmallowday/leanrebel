# ReBeL status — M06 in progress; M05 accepted

## Active information-set backend controls

Continue on rebel/m06-information-controls-20260921; read its actual remote
HEAD and CI. Its parent c669ef769f1b833fadd8b728b6950b877f6f52be is retained on
rebel/m06-information-contract-20260921, so its in-flight validation is not
cancelled by the new controls. The actual starting source was
30f7dfd33c05a78a70e1a29cb2734cde1af90a02, not the old b0564668 chat checkpoint.
All reverse-decoder and original-PBS information-set CFR work is inherited.
M06-status-before-information-contract.md preserves the inherited STATUS.
Main stays 6a6cbdaa8b4fb43b35a9d3e555a7c1a614130098. No force update or integration.

## Checkpoints and validation boundary

5303cde4 records the restart. 6ee6ae40 fixes one 101-column line without changing
its proposition or proof; target run 35558360214/job 106206318768 succeeded.
f64cd0b6 adds the finite information-set budget, registered at 90945539.
That source's target 35559328728/job 106209075746 found exactly one compiler
error: the payoff-budget proof used the addition lemma in the wrong order.
a68aea2f repairs that application. Its subsequent target was cancelled by
the later source push; partial steps are not accepted as complete validation.
c669ef76 adds and registers the information-set child, completion and parent
backend. Its exact-source checks are in progress, not claimed successful.

## Constructed candidates

PBSInformationBudget computes a positive finite information-set CFR count
from its actual finite-schedule coefficient and a prescribed positive error.
CFRDInformationChild solves the factual joint posterior, splices by public cut
prefix, and preserves every baseline and unilateral continuation law.
CFRDInformationContinuation derives the factual probability budget and uses
the existing zero-own-reach response to establish every parent leaf query.
CFRDInformationDriver supplies these children to the actual perturbed parent
recurrence and derives approximate Nash and private carried-policy security.
See M06-information-contract-construction.md for the exact semantic boundary.

Eleven new canonical hidden-type controls cover a positive round count, a live
budgeted solve, the actual child-table branch, joint prefix preservation,
a randomized unilateral law, absent queries, zero remaining fuel, all-query
completion, every parent iterate, nonzero numerical bias and full-game Nash.
All new modules are in the umbrella, targeted build and supplemental lint and
transitive-axiom consumer. All prior targets, imports, tests, dependency pins,
coverage rows and validation gates are retained. Compiler acceptance is pending.

## Remaining original M06 obligations

The backend uses actual information-set CFR, but its child count depends on
its joint-posterior mass budget. Zero-own-reach completion and positive child
loss are explicit; no common positive learned-mass floor is assumed.
The constructed bound separates prediction error, outer finite-T residual and
2*childLoss for private selected-policy carried execution. It does not identify
that fixed family's play with independently re-solved recursive child families.
Construct that policy/value correspondence and original test-time guarantee;
do not assume arbitrary Nash replacement is lossless against a fixed opponent.
The real-valued reference is not an executable rational refinement. Keep all
counterexamples, off-model distinctions and printed/corrected Theorem 3 intact.
Original SEARCH-FRONTIER, SEARCH-CFRD, SEARCH-ERROR and SAFE-THEOREM3 stay pending.
No M06 or complete-framework acceptance is claimed.
