# ReBeL status — M06 in progress; M05 accepted

## Active implementation restart

Continue on rebel/m06-coherent-recursion-20260921; read its actual remote HEAD.
Its predecessor e965412ccad7ee42f03e50a4d6b6dfb832c3c3d1 remains on
rebel/m06-coherent-child-20260921 so its ongoing checks are preserved.
The validated inherited base is ff7f61510c38a5681c06a7431a7811e3f6fbcbf7;
full repository CI 35539749429/job 106155158791 and full ReBeL validation
35539749435/job 106155159018 passed. The previous baseline STATUS is preserved
in M06-status-before-coherent-child.md. Main remains
6a6cbdaa8b4fb43b35a9d3e555a7c1a614130098. No history rewriting or main integration.

## Saved implementation and current validation boundary

The predecessor's 71-target compilation step passed in 35544309611/job
106167437931. Its supplemental validation was still running when this next
stage was prepared. Re-read its result; do not label a step a complete run.
This new source adds repeated coherent sampling and needs its own validation.

CFRDCoherentDraw predraws finite information-local plans of one fixed computed
behavioral profile. The complete unilateral law holds at every legal root,
including off-model roots, not just at a model posterior's support.
CFRDFiniteCoherent instantiates the actual perturbed finite-child outer trace
and derives its opponent envelope and security without a replacement premise.

CFRDCoherentStages uses the canonical recursive memory/belief state machine.
Every active stage redraws plans from the original private parent index;
all prior draws and model-posterior updates remain in the state. A finite
schedule covers all remaining execution fuel. Exact whole-law equality and
the finite-child outer security bound have no stage-count penalty. Eight
example theorems include two live strategic rounds separated by a zero-fuel
stage, arbitrary private memory, a randomized source unequal to any drawn
pure profile, and the actual biased finite-child trace.

All five modules are registered in the umbrella, 73 targets and supplemental
35-module lint/axiom consumer. No source or old target/import was removed.
The original coverage ledger, pins, architecture and verification gates remain.

## Semantic scope and remaining original M06 work

This is COHERENT EXECUTION REFINEMENT of the existing computed child family.
It does not independently solve a new later PBS game or identify predrawing
with uniform child-CFR-iteration sampling. Individual drawn pure policies
need not be safe; the proved statements quantify their actual execution laws.
No equality of carried MODEL PBS and unknown opponent's true law is assumed.

The original paper's recursive information-set CFR/value-family correspondence
remains open. The finite solver is still an adaptive real-arithmetic complete-
plan reference. Prediction error, positive child loss, outer finite-T error and
optional bounded-refresh losses remain separate. All original SEARCH-FRONTIER,
SEARCH-CFRD, SEARCH-ERROR and SAFE-THEOREM3 parents stay pending. No M06 acceptance.
