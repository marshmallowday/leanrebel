# ReBeL status — M06 in progress; M05 accepted

## Active sampled-parent continuation

Resume from actual remote HEAD of `rebel/m06-sampled-parent-20260923`.
Main remains accepted M05 at `6a6cbdaa8b4fb43b35a9d3e555a7c1a614130098`.
No main merge or history rewrite. Original hashed M06 coverage remains pending.

## September 23 restart: exact-source architecture repair

The previous HEAD `5d7386fe75541819f6f75e955d6c26216c9f39c3` failed the
ReBeL architecture gate in run `35814842881`, job `107034060328`:
`TRANSPORT_ANALYSIS_SOURCE: expected 0, got 1`. The same log confirms that
all library line widths passed. Its source snapshot is artifact `10731240831`,
with inner tar SHA256
`17d22e571d29d16e196af775eba9bd6b281ddb6d1129a0f61eb536cfb74439f1`.

The extra transport was the `change` tactic in
`informationSamplingControl_zero_query_sampled`. Replace it with a direct
proof of the same reducible nonterminal proposition. No test statement,
source obligation, dependency, linter, architecture expectation or axiom
allowlist is weakened. This checkpoint still requires its own target-SHA
compiler/lint/axiom results; inspect the new push workflows before acceptance.

Earlier `e966701836d809cc1b197bce95f874d3a4e01771` repaired the general
completed-query scripts; targeted run `35814111823`, job `107031840693`,
compiled that general module before exposing the concrete control's
previous missing-decidability error. Do not repeat those completed repairs.

## Preserved construction and next slice

Factual private/live queries retain actual child iterations with their
public-posterior-dependent count. Genuine reference queries with zero factual
mass use computed response completion. Their laws realize the same completed
continuation against arbitrary fixed opponents with positive child loss.
Factual-zero, supported-root, independent-index and zero-fuel controls remain.

Next connect these sampled query values to the actual noisy parent recurrence,
then independently re-solved recursive carried-PBS execution. Do not identify
model and actual posteriors or assert individual-iterate optimality. Preserve
positive child/prediction losses, finite outer T, and the printed/corrected
Theorem 3 distinction. One-PBS or one-query identities are not recursive safety.
`SEARCH-FRONTIER`, `SEARCH-CFRD`, `SEARCH-ERROR`, `SAFE-THEOREM3` remain pending.
No numerical-refinement or complete-framework claim.
