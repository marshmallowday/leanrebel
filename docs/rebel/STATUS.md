# ReBeL status — M06 in progress; M05 accepted

## Current complete replay candidate

Work branch: rebel/m06-query-replay-20260925.
Read M06-query-replay-validation.md and M06-query-replay-coverage.json.
Core source2973770a358ef63c94ada29a3d52d1f33f16d164 remains fixed on
rebel/m06-signed-query-20260925 while this child commit validates the consumers.
Its target135 compiled; supplemental audit was still running at assembly.
Exact-SHA success of this complete candidate must be checked before acceptance.

The prior independent ReBeL run36103763078/job107971751432 has now succeeded
on source4f73dcefb0d9f0e0b24b5a390a754e6debbdd0e5, with4718 transitive
declarations and241 modules in the inspected log. Preserve its value-coupling
checkpoint2e1de273b9164743decdd8a283b3249f34bf5600 and all earlier evidence.
M06-signed-query-progress.md records the artifact identities and hashes.

## New scope and verification

Three replay modules are now in the public analytic umbrella, all137 target
entries and99 supplemental lint/axiom modules. The complete implementation
computes the same finite information child from a restored original prefix,
derives zero signed supported-query drift for actual noisy parent rounds,
retains the actual seed/history law through one fresh solve plus a coherent
private-plan draw, and transfers the original finite-T/noisy security bound.
No small-drift assumption, output equality or child-Nash field is supplied.

Native retained-iteration query equality and coherent-plan actual execution
are separate results. Equal joint prefix law is essential; public marginal
equality alone fails the nondegenerate negative control. Tests include the
actual biased hidden-type parent and a sampled but factually absent query.
Offline ledger/inventory and76 Python tests pass after fixing public umbrella
registration; exact-SHA Lean compilation, lint and axiom audit are still pending.
No inherited gate, pin, workflow, trust allowance or negative test is weakened.

## Remaining M06 work

This is SAME cut, SAME fallback/payoff/horizon/bound/child tolerance and one
recomputed child. Do not claim a stability rate for different configurations,
independent later carried-PBS re-solving or native first-exit sampling.
CarriedResolveStepBounds and those native obligations remain open. SEARCH-FRONTIER,
SEARCH-CFRD, SEARCH-ERROR and SAFE-THEOREM3 remain pending. M06 is NOT complete.
Keep finite-T error at zero oracle error and the printed/corrected Theorem3
distinction. Main and accepted predecessor branches are unchanged.
