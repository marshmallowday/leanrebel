# ReBeL status — M06 in progress; M05 accepted

## Active implementation restart

Continue on rebel/m06-coherent-child-20260921; read its actual remote HEAD.
Base ff7f61510c38a5681c06a7431a7811e3f6fbcbf7 passed full repository CI
35539749429/job 106155158791 and full ReBeL validation
35539749435/job 106155159018. These are baseline successes, not acceptance
of the new source. The previous STATUS is preserved byte-for-byte in
M06-status-before-coherent-child.md. Main remains
6a6cbdaa8b4fb43b35a9d3e555a7c1a614130098. No history rewriting or main integration.

## Saved implementation, awaiting its own compiler and audit results

CFRDCoherentDraw independently predraws each finite information-local plan
from one fixed computed behavioral profile. Its marginal and every-root
unilateral law use the canonical finite-predraw theorem. The carried resolver
has exactly the parent's history law, including off-model roots and stopping.
It derives the opponent-reference envelope from that same parent's child
contract, rather than assuming a local comparison for a new solver output.

CFRDFiniteCoherent specializes the family to the ACTUAL noisy outer recurrence
and its already constructed finite, posterior-budgeted, completed child solves.
The resulting security statement has A*predictionError+C/sqrt(T)+2*childLoss
with no switch rate or extra replacement loss. Its proof needs this head's
successful compilation, lint, transitive-axiom output and semantic review.
Six new examples include a fair source that equals none of its deterministic
drawn profiles, an off-model live root, zero fuel, and the actual biased
finite-child trace's law, envelope and security.

All three modules are in the analytic umbrella, target list and supplemental
audit. Every inherited target/import/gate and dependency pin is retained.
The source includes no placeholder proof or custom axiom. Do not infer success
from source presence or from a successful parent checkpoint.

## Scope and remaining M06 construction

This is a COHERENT EXECUTION REFINEMENT of the existing computed finite-child
family, not a new independent solve at a different later PBS. Recomputing the
same deterministic mathematical family is permitted; changing the family is
not justified by this theorem. The source's random-iteration recursive
information-set CFR correspondence remains to be constructed. Predrawing
realizes a behavioral result; it is not asserted equal to sampling the child
regret iterations, or evidence that every pure sampled plan is individually safe.

No original SEARCH-FRONTIER/SEARCH-CFRD/SEARCH-ERROR/SAFE-THEOREM3 parent is
promoted. Numerical error, finite child loss, outer finite-T error and the
separate bounded-refresh penalty retain their distinct meanings. Preserve
the printed/corrected Theorem 3 distinction and all replacement counterexamples.
