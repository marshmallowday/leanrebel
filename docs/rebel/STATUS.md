# ReBeL status — M06 in progress; M05 accepted

## Resume point

Use the actual remote HEAD of `rebel/m06-nested-depth-20260924`.
Its parent is704e96ff6ed5b73b279191ea2d01629cc331e8d8, preserving the first-exit
source487aaedd, review0309f7c0 and integrated finite-budget checkpoint e9d651b2.
The separate budget-integration branch remains unchanged during nested checks.
Main remains6a6cbdaa8b4fb43b35a9d3e555a7c1a614130098; do not write main.
Inspect the exact HEAD's compiler/audit results; never infer pending success.

## Accepted predecessor and recovered budget source

First-exit source487aaedd passed M06 target35941683756/job107450728931,
ReBeL35941683758/job107450798403, full CI35941683736/job107450816152 and
inventory35941683774. This supersedes earlier pending review snapshots.
Support recovery01245b31 remains accepted separately. Do not repeat either.

An existing divergent branch at2c9bfd52 contained the finite depth-budget code
and controls without import/target/audit wiring. Integration e9d651b2 reused
its two exact new blobs and added only the missing consumers, retaining the
newer repaired first-exit code rather than restoring that branch's stale files.
Target35945479753/job107462340355 then failed at redundant ring, undistributed
player-bound algebra and an unfolded-round mismatch. Repair704e96ff fixes those
proof steps only. It still needs its own inspected validation; no theorem,
predictor, budget, heartbeat, dependency or validation gate was weakened.

## New candidate: actual depth-limited children in the coupled parent

CFRDDepthChild runs the allocated depth solver at each current factual joint
PBS, proves baseline and unilateral full-law preservation under public splicing,
and derives the all-query counterfactual child contract after zero-own-reach
completion. CFRDNestedDepthDriver uses that continuation in each actual noisy
parent update, deriving finite-T all-deviation Nash and private carried security.
Outer numerical noise and the child predictor's positive allowance are distinct.
No child Nash or local optimality certificate is an input. Concrete controls
include nonzero outer/child biases, positive child loss, legal fallback cases,
a randomized deviation and finite parent iteration counts. See M06-nested-depth.md.

These three NEW modules are connected to the analytic root, declared M06 targets
and the supplemental normal/slow lint and all-declaration axiom audit. This
candidate remains UNACCEPTED until its exact-SHA compiler and audits succeed.
The first-exit, budget and all prior tests remain in every existing consumer.

## Remaining original obligations

Generalize the constructed single nesting step to arbitrary-depth structural
recursion; the deepest backend still uses full-root CFR. Derive source-level
model-value drift and useful first-exit/support-defect estimates and discharge
CarriedResolveStepBounds. Private carried security is not independent recursive
re-solving security. No model/actual posterior equality, learned-network accuracy
or executable numeric-refinement theorem is asserted. Retain finite outer T,
positive numerical/child errors, zero-reach completion and the printed/corrected
Theorem3 distinction. SEARCH-FRONTIER, SEARCH-CFRD, SEARCH-ERROR and SAFE-THEOREM3
remain pending; source qualifications, ledgers and M05 acceptance are unchanged.
