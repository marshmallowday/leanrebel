# ReBeL status — M06 in progress; M05 accepted

## Active independent and supported sampling integration

Continue on rebel/m06-sampling-validation-20260921; read the actual remote
HEAD and its exact-source CI before editing. The integration has parents
77cc57d3575b48762c174e2e6ee3912e7d5527ab and
28f789980d055943cffc171a3be2559744d2e3e4. It preserves both lines of work.
Main remains 6a6cbdaa8b4fb43b35a9d3e555a7c1a614130098, accepted through M05.
No force update or main integration was performed.
The supported-sampling STATUS is preserved byte-for-byte in
M06-status-before-independent-sampling.md. Its pending checks are historical,
not evidence of a completed validation of this integrated source.

## Construction retained and added

The existing PBSInformationSampling and CFRDInformationSampling expose the
actual information-set CFR iterations and connect the private uniform
retained index to the child's own-reach average and parent public splice.
PBSSupportedSampling separates complete-history root fibers and establishes
the same one-player law at each positive-mass model root and every supported
reweighting. All these declarations and their existing controls are retained.

The integration adds pbsInformationCFRIndependentSample and two general
law/value theorems. Each player independently draws its own uniform index
from the actual same-T child recurrence and retains it for the continuation.
Two applications of the unilateral law and the established profile-update
identities derive the entire self-play history law of the computed average.
The value theorem applies to every real original-history observable.

Three new controls instantiate a live two-iteration solve, preserve the root
law at zero continuation fuel, and distinguish the product index distribution
from the diagonal shared-index distribution. The last control concerns the
index law; it does not assert a payoff difference in every game.

## Validation boundary

This merged source requires new build, lint, architecture and axiom results.
The isolated sampling workflow prints exact source and signatures and runs
both normal module lint and the full registered supplemental axiom/lint audit.
Existing full repository and ReBeL workflows remain unchanged. No checks,
allowed axioms, theorem statements or dependency pins are weakened.
See M06-sampling-validation-restart.md for the corrected failure provenance.
No local compiler execution, independent archive hash or new numerical
refinement is claimed.

## Remaining original M06 obligations

Supported reweighting is already present; do not redo it. The remaining
construction must combine actual child iteration families with the parent's
counterfactual/type-conditioned queries and zero-own-reach completion, then
prove independently re-solved recursive carried-PBS play against a fixed
unknown opponent. A one-PBS law and one-cut security are not that theorem.

The model PBS is not assumed to equal the opponent's actual posterior.
No positive-fuel guarantee outside model support, per-iterate Nash guarantee,
per-action redraw equivalence or lossless arbitrary Nash replacement follows
from this slice. Keep positive prediction/child errors, outer finite-T terms,
posterior-dependent child budgets and printed/corrected Theorem 3 distinct.
SEARCH-FRONTIER, SEARCH-CFRD, SEARCH-ERROR and SAFE-THEOREM3 remain pending
in the unchanged content-hashed coverage ledger. No M06/framework acceptance.
