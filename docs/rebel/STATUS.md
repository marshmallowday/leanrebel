# ReBeL status — support-rate proof repair; M06 incomplete

## Current branch and source boundary

Continue `rebel/m06-support-rates-20260927`, based on the latest combined
integration `7326f1e40011b1d4329f09e743491cc7bc7e5746` from
`rebel/m06-conditioned-query-20260925`. The first continuation checkpoint is
`3de54cc88fe0ccb87e2aa603c4d833ac5a3d1662`. The feature source is
`f14ec3bddd5e61ba3e4eb505cd2c73a37f7f3068`; this checkpoint repairs its
set-indicator normalization proofs without changing any theorem statement.
The separate branch preserved the original integration audit from concurrency
cancellation; that baseline audit has now succeeded and been inspected.
Both integration parents and all earlier implementations are retained.
Main remains accepted M05 at `6a6cbdaa8b4fb43b35a9d3e555a7c1a614130098`.
M06 and paper Theorem 3 are NOT complete. M07 is not started.

## Current dependency-closed change

The support-only charge derives carried posterior support-failure rates from
incoming unsupported mass plus actual-visit one-step support leakage. Unlike
full source-atom variation, differing positive weights inside model support
cost nothing. The candidate proves finite-horizon propagation, comparison with
the old charge, and zero charge under explicit primitive support inclusions.
It includes the actual budget-1/8 information-set CFR child and hostile
finite-law controls. No law equality, changed-opponent convergence, support
floor or child Nash certificate removes a genuine support failure.

Read M06-support-rates.md, M06-support-rates-coverage.json and
M06-support-rates-validation.md. Original statements and tests remain.
The 110 target modules, 253 global modules, imports, auditors, workflows,
dependency pins and 3,054 original source items are unchanged. Prepared-source
Python discovery passed 112 tests including 16,384 new exact-rational cases;
f14's actual source-inventory workflow also passed all 112. Neither is Lean
proof evidence. No local Lean or PowerShell execution is claimed.

## Compiler failure and next action

The actual f14 target artifact was inspected: run 36277385486, job
108502646650, artifact 10916689581. FinDistKernelVariation failed on a missing
not-false simplification and differences between set-membership and predicate
indicators. It did NOT pass the target compiler or reach downstream proof
validation. This source repairs only those proof expressions, with no linter
suppression or changed hypotheses. Inspect the REPAIR source's exact-SHA
target, normal/slow lint, transitive axioms, global ReBeL, full CI and inventory
next; all repair-source gates are currently pending.

The combined baseline 7326f1e4 now has inspected success for the target
36275563682 / 108497496370 (110 lint passes, 1,739 allowlisted records), full
CI 36275563654 / 108497657992 (full build, lint and architecture), and global
ReBeL 36275563642 / 108497538005 (253 lint passes, 4,910 allowlisted records).
These are baseline results, NOT validation of the support-rate candidate.
Exact artifacts and hashes, failed-attempt evidence and resume IDs are in
M06-support-rates-validation.md. Do not repeat accepted baseline work or call
any repair proved merely because the underlying mathematical bound is sound.

## Remaining semantic boundary

This is a fixed-profile finite continuation support bound, not a completed
first-exit theorem for independently re-solved carried schedules. Support-only
zero charge requires actual one-step support dominance; it is not automatic
for arbitrary unknown opponents. Small accumulated leakage for the full
algorithm remains a separate obligation. Being in a PBS support is not equality
between the stored model PBS and the actual conditional distribution.

Native gaps retain original compatible joint type kernels and the SAME
computed average comparison opponent. Public conditional rates retain their
actual event-mass denominator. Changed-PBS native/late rates, full recursive
re-solving safety and CarriedResolveStepBounds remain unproved.
SEARCH-FRONTIER, SEARCH-CFRD, SEARCH-ERROR and SAFE-THEOREM3 remain pending.
No learner convergence or dropped finite-T residual bypasses test-time safety.

M07 onward retains the user's one-integration-branch-per-milestone policy.
This task does not start M07, delete older branches, force-push or update main.
