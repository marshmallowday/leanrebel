# ReBeL status — M06 in progress; M05 accepted

## Active verified information-set contract checkpoint

Continue on rebel/m06-information-checkpoint-20260921; read its remote HEAD.
The verified proof source is 6416eebd9a67645fe94100db744cd4d049d17684, preserved
on rebel/m06-information-controls-20260921. Evidence changes documents only,
leaving that source's full CI runs undisturbed. The parent-contract source
c669ef769f1b833fadd8b728b6950b877f6f52be remains on
rebel/m06-information-contract-20260921.

The actual restart source was 30f7dfd33c05a78a70e1a29cb2734cde1af90a02. It
already contained reverse decoding and original-PBS information-set CFR.
Do not replay old b0564668 or failed decoder patches. Seven descendant commits
save the restart, inherited line-width repair, finite-budget construction,
registration, one payoff-inequality repair, parent contracts and controls.
M06-status-before-information-contract.md preserves the inherited STATUS.
Main stays 6a6cbdaa8b4fb43b35a9d3e555a7c1a614130098. No force update or integration.

## Exact-source validation inspected

At 6416eebd, run 35560410070/job 106212097997 completed every step successfully.
All 88 declared M06 targets built in 3,421 Lake jobs, followed by a 3,425-job
supplemental build, all 50 normal/slow module lint passes and 568 complete
transitive-axiom records. The five new modules own 44 records; all use only
propext, Classical.choice and Quot.sound. The source adds 18 public general
theorems, one private helper and eleven example theorems. These counts are
not interchangeable. No Lean error/warning occurs; Actions separately reports
Node deprecation warnings. See M06-information-contract-validation.md and
the explicitly normalized 44-record M06-information-contract-axioms.txt.

Source inventory 35560410181/job 106212098735 passed. Full repository CI
35560410121/job 106212150505 and full ReBeL 35560410133/job 106212156566 were
still running at initial evidence recording. The full build, reuse checks and
architecture phases 1/2 passed. ReBeL width, static architecture, ledger,
inventory/adversarial fixtures and existing rational execution passed.
Those intermediate steps are not final full-workflow success. Re-read exact-
source final results before integration; the evidence head has separate runs.

The complete target job log was read through the GitHub plugin. Final archive
digests are labelled server-reported; no final local compile, Python suite or
independent final archive hash is claimed. Existing proofs, imports, targets,
tests, dependency pins, coverage rows and validation gates were not removed
or weakened. The three validation consumers gained only five module entries.

## Constructed information-set child and parent guarantees

PBSInformationBudget computes a positive finite count from the actual
information-set CFR schedule coefficient and a requested positive tolerance.
It runs that recurrence and decodes its output into the original joint-PBS game.
CFRDInformationChild splices these actual factual-PBS solves by public cut
prefix and preserves the complete baseline and unilateral continuation laws.
CFRDInformationContinuation derives the type-mass-scaled reference budget and
uses existing zero-own-reach completion to establish every parent leaf query.
CFRDInformationDriver connects the actual perturbed parent sequence to those
children, deriving full-game approximate Nash and private carried security:
V_reference - ((A0+A1)*predictionError + (C0+C1)/sqrt(outerT) + 2*childLoss).
No child Nash, positive-mass-floor or conditional-quality certificate is supplied.
The comparison equilibrium only names the reference value.

Eleven canonical hidden-type controls cover the positive count, live budgeted
solve and actual table, joint prefix, randomized unilateral law, unvisited
query, zero remaining fuel, all-query completion, parent iterates, nonzero
numerical bias and full-game Nash. They are checked theorem fixtures, not a
numerical refinement of the new real-valued solver. The input reduced-model
fallback is explicitly lifted; learned policies and deviations use full AOHs.

## Remaining original M06 obligations

This backend uses information-set CFR rather than complete-plan normal-form
learning, but the child count depends on the actual posterior and its positive
child tolerance. Small posterior mass may require a large count. No uniform
positive learned-mass floor or common fixed child-T is asserted. The exact
zero-own-reach response is an explicit part of the construction.

Construct the original recursive parent/child information-set policy/value
families and their correspondence to independently re-solved carried-PBS play.
The verified one-cut parent/carried guarantee and fixed-family coherent redraw
are not that independent recursive guarantee. Do not assume arbitrary Nash
replacement is lossless against a fixed opponent, or identify a model PBS
with the unknown opponent's true law. Preserve all counterexamples, positive
prediction/child errors, outer finite-T residual and printed/corrected Theorem 3.
The real-valued reference is not an executable rational refinement.

M06-information-contract-review.md records coverage and semantic review.
Original SEARCH-FRONTIER, SEARCH-CFRD, SEARCH-ERROR and SAFE-THEOREM3 remain
pending in the unchanged content-hashed ledger. No M06/framework acceptance.
