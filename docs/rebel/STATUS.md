# ReBeL status — native-gap global audit passed; M06 incomplete

## Resume checkpoint

Continue on `rebel/m06-joint-query-20260925`, based on the validated source
`e65152038f6a9068be68aff28ad6845f74097e94` from
`rebel/m06-native-gap-checkpoint-20260925`. This first checkpoint records
previously pending validation; it does not change Lean sources.
Read M06-native-gap-global-validation.md before repeating any accepted work.
Main remains `6a6cbdaa8b4fb43b35a9d3e555a7c1a614130098` and is not advanced.
M05 is accepted. M06 and Theorem 3 are NOT complete.

## Resolved predecessor gate

Exact-source ReBeL run `36131219015`, proof job `108058664709`, completed
SUCCESS. The actual exported log starts with e65152038f6a9068be68aff28ad6845f74097e94,
passes 247 modules and 4799 transitive declaration sets, and includes both
native conditional-gap modules' individual normal/slow lint and axiom output.
Only propext, Classical.choice and Quot.sound occur in the recorded native
journal anchors. Full repository CI `36131219021`, job `108058665488`, also
completed SUCCESS, including build, configured lint, all architecture gates,
reuse signatures and cleanliness. Artifact IDs and hashes are retained in
the new validation document. This resolves the pending observation in the
older native-gap validation checkpoint, without changing its historical log.

## Next dependency-closed slice

Extend the fixed-slice, fixed-average-opponent native gap bound to an explicit
JOINT law of retained private iteration and queried type. A bounded joint
Radon-Nikodym ratio can depend on BOTH coordinates. Also connect a tagged
seed-dependent query kernel, and retain a concrete equal-marginal/correlated
counterexample and zero-support boundary. Test the bridge with the actual
information-set CFR budget interface. No such law or density is yet inferred
for an arbitrary independent re-solve or actual carried posterior.

The original native mean theorem remains a product-law theorem. Its proof
uses the nonnegative current-opponent best-response gap, not signed
cancellation. Its uniform private iteration is drawn once and retained;
there is no shared opposing seed or resampling at each move. Compatible
zero-mass conditional kernels and the finite-T residual remain visible.

## Remaining M06 obligations

SEARCH-FRONTIER, SEARCH-CFRD, SEARCH-ERROR and SAFE-THEOREM3 remain pending.
Independent changing-opponent/changing-PBS native and late rates, actual
seed-correlated carried support/first-exit and quantitative joint density
control, independent recursive re-solving safety and CarriedResolveStepBounds
remain. Preserve all earlier source qualifications, tests, repair records and
exact-SHA evidence. In particular learner convergence must not replace the
independent test-time proof, and oracle error zero must not remove finite-T error.
