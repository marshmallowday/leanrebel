# ReBeL status — M06 in progress; M05 accepted

## Active verified dependency-slice checkpoint

Continue on `rebel/m06-pbs-checkpoint-20260921`; read its actual remote HEAD.
The proof source is `d693b30eddbf5a2d523c92d2308f51879f4c28d4`, retained on
`rebel/m06-pbs-cfr-20260921`. This checkpoint changes evidence and status only,
leaving that source's independent full CI runs undisturbed. Its base is the
coherent checkpoint `8c065784a76ad1de8fc6542dad336f4edfe9f66f`, whose proof
source is `93b60fbdc3fe6539a7688124baa9be80b86602c1`. All inherited M06 work
is present; do not replay superseded repairs. Main remains
`6a6cbdaa8b4fb43b35a9d3e555a7c1a614130098`, accepted through M05.
No force updates, history rewriting or main integration were performed.
`M06-status-before-pbs-root.md` preserves the previous coherent STATUS exactly.

## Exact-source validation actually inspected

At d693b30e, target run `35550519612`, job `106184279972`, completed every
step successfully: 79 declared M06 targets, 3,405 Lake build jobs, all 41
supplemental module lints and 468 unique complete transitive-axiom records.
All dependency lists contain only propext, Classical.choice and Quot.sound.
There are no Lean error or warning records in the inspected target log.
The six new modules own 112 audited records, 23 general theorems and seven
example theorems. `M06-pbs-root-validation.md` records source, archive, log
hashes and repair provenance. `M06-pbs-root-axioms.txt` is a labelled normalized
excerpt containing all 112 new-module records, not the complete raw audit log.

The exact source snapshot matches all six new modules and the three expanded
registration/auditor files. All 76 existing Python tests pass with warnings as
errors. Ledger and inventory structure checks pass, and the 517 non-Experimental
Lean files have no line-width violations. Every inherited import, target and
all 3,054 expanded coverage rows remain. Dependencies, workflows, allowed
axioms and architecture gates are unchanged; new modules were added to checks.

Full repository CI `35550519628` / `106184318263` and full ReBeL
`35550519618` / `106184325607` are still running at this evidence checkpoint.
The latter's width, static architecture, ledger/inventory/adversarial fixtures
and independent rational Lean runtime checks have passed. This intermediate
state and the supplemental success are not full-workflow success. Re-read
those exact-source results before integration. The evidence head has separate
checks. M06 itself is not accepted as complete.

The inherited 93b60fbd source has passed full repository CI `35545208101` /
`106169800893` and full ReBeL `35545208160` / `106169767571`; actual completed
jobs were checked. This supersedes the old coherent STATUS's pending note.

## Constructed PBS-rooted information-set CFR

PBSRootProtocol samples the entire joint law of legal original histories in
one genuine chance step, then retains original transitions and histories.
A strict history-length rank derives bounded horizon and finite rooted
histories. PBSRootInformation exposes original public observations and only
each player's own local information; the solver uses original full AOHs and
the complete subsequent local sequence, not a hidden-world policy argument.

PBSRootCFR constructs the existing fixed-positive-T information-set regret-
matching recurrence and own-reach average. Its native rooted-game approximate
Nash theorem uses the actual two-player cumulative CFR bounds divided by T.
Finiteness, local clock, perfect recall, fallback and payoff properties are
constructed. No child Nash or regret certificate or root independence is
assumed. This is not the preceding complete-plan normal-form learner.

PBSRootExecution proves equality of complete original-history laws under the
canonical randomized runner at all finite fuels, accounting for exactly one
initial chance step. PBSRootBehavioral connects independent local policies
and preserves every original full-AOH behavioral profile and every unilateral
replacement. The seven controls cover a genuine live factual posterior, a
randomized deviation, full-AOH hidden-bit indistinguishability, joint-root
correlation, terminal roots and zero administrative fuel.

## Remaining original M06 obligations

The proven policy correspondence is ORIGINAL-TO-ROOT. Construct the reverse
information-local decoder for arbitrary new rooted CFR output at an actual
PublicBelief, using its common public cut rather than arbitrary mixed-depth
roots. Prove baseline and every unilateral law and expectation before moving
the native approximate Nash theorem to the original PBS continuation game.
Then construct the parent's counterfactual/type-conditioned value and iteration
families and their correspondence to independently re-solved recursive play.
A native rooted-game Nash theorem alone does not establish that safety result.

The inherited finite-plan child backend and coherent redraw guarantees remain
intact. The new abstract real-valued reference is not an executable rational
refinement and does not identify coherent redraws with independent re-solving.
Preserve positive prediction/child errors, finite-T residuals, off-model and
zero-reach cases, and the printed-versus-corrected Theorem 3 distinction.
`M06-pbs-root-coverage.md` records the dependency and semantic review.
Original SEARCH-FRONTIER, SEARCH-CFRD, SEARCH-ERROR and SAFE-THEOREM3 remain
pending in the unchanged content-hashed ledger. No framework completion claim.
