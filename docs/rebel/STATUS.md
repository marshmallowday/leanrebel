# ReBeL status — M06 in progress; M05 accepted

## Active actual-iteration sampling restart

Continue on rebel/m06-sampling-verification-20260921 and read its actual HEAD.
The current proof candidate is 005baa8229d4307d372cd359a41a47762d55d41d,
retained on rebel/m06-sampling-documentation-20260921. This checkpoint only
updates documents so that the source's separate verification is not cancelled.
The inherited verified checkpoint is af6f600727b0c50906155c54f23a26e7529426fd,
with proof source 6416eebd9a67645fe94100db744cd4d049d17684. Its full ReBeL
run 35560410133/job 106212156566 completed successfully. All inherited decoder,
information-child budget, parent contracts, zero-own-reach completion and
coherent redraw work is present. Do not restart from superseded repairs.
Main is unchanged at 6a6cbdaa8b4fb43b35a9d3e555a7c1a614130098.
M06-status-before-information-sampling.md preserves the inherited STATUS.

## Exact validation boundary

At source 72a837da, all 93 declared targets compiled and all 648 transitive
axiom records passed, but supplementary lint FAILED on two generated proof
witnesses. Making the native implementation private did not fix their docs.
The following 3989e2d3 attempt failed compilation because add_decl_doc could
not resolve those generated witnesses at the documentation commands.
Neither intermediate commit is an accepted complete validation checkpoint.

Current source 005baa82 restores the direct public native iterate, uses
synchronous elaboration in the two new implementation modules, and documents
the generated witnesses without disabling lint or any kernel/axiom check.
Its target run 35566614048/job 106229612027 and full ReBeL run
35566614312/job 106229674353 are in progress at this recording point.
Read their final results and the exact-source full repository CI before
acceptance or integration. There are 93 targets and 55 supplementary modules.
No local compiler, local Python test run or independently computed archive
hash is claimed. GitHub operations and exact CI reads use the plugin.

## New implementation, with scope unchanged by the metadata repair

PBSInformationSampling exposes the actual cfrPlay iterations used by the
child's own-reach average. A private uniform iteration index is retained
through the continuation, against fixed arbitrary behavioral opponents.
The native/rooted and original PBS laws agree on complete histories, and
training fuel is separate from executed continuation fuel.

CFRDInformationSampling connects the same posterior-budgeted iteration draw
to the factual child table and the actual public-prefix-spliced child policy.
PBSSupportedSampling separates complete-history fibers at the common cut,
proving equality at each supported root and under any support-included
reweighting or conditioning. Actual and model probabilities may differ.
The original prefix readout is a proof device, never a hidden policy input.

The five new modules contain fourteen public general theorems, three private
proof helpers and twelve example theorems. Controls include live and randomized
opponents, the exact T=2 draw, positive child loss, zero fuel, the actual parent
splice, root conditioning and a zero-mass cancellation counterexample.
These counts are not compiler-generated axiom-record counts.

## Remaining original M06 obligations

Connect the actual sampled child family to the parent's conditional and
counterfactual reference queries, including the existing omitted-own-type
response completion. Then prove independent recursive carried-PBS re-solving
against a fixed unknown opponent. A supported-root identity does not cover
new actual roots outside model support. Independent re-solving is not
fixed-family redraw, arbitrary Nash reselection or per-action resampling.

Preserve prediction error, positive child tolerance, outer finite-T residuals,
zero-reach cases and the printed/corrected Theorem 3 distinction. The actual
posterior-budgeted real-valued child is not an executable numerical refinement
or a common fixed child-T solver over every possible posterior.
M06-information-sampling.md and M06-information-sampling-review.md describe
this dependency slice. The unchanged content-hashed SEARCH-FRONTIER,
SEARCH-CFRD, SEARCH-ERROR and SAFE-THEOREM3 parents remain pending.
No M06 acceptance, main integration or complete-framework claim.
