# ReBeL status — M06 in progress; M05 accepted

## Current continuation

Work branch: rebel/m06-signed-query-20260925.
Start checkpoint: fa4d71e14706718ecde9f680b8d87923c821343a.
Read M06-signed-query-progress.md and M06-query-replay-coverage.json.
The complete preceding STATUS remains at immutable parent
2e1de273b9164743decdd8a283b3249f34bf5600.

The previously pending independent ReBeL run36103763078/job107971751432
has succeeded on source4f73dcefb0d9f0e0b24b5a390a754e6debbdd0e5.
Its downloaded exact-source log prints4718 transitive declarations and241
validated modules. Artifact identity and hashes are in the progress record.
Accepted source and checkpoint refs are unchanged; main is unchanged.

## New source checkpoint, not yet accepted

CFRDQueryReplay derives exact same-setting recomputation from the actual
finite information-CFR child's dependence on its joint cut law and preserved
prefix. This also preserves the computed off-path response, actual retained
child-iteration queries and every actual noisy parent round's continuation.
No output equality, small drift or scalar Nash premise is supplied.
The next step is exact-SHA compiler feedback, then supported signed-value,
actual-law/security consumers and positive/negative examples. Add the public
umbrella connection with that accepted dependency-closed slice.
The M06 target and supplemental lint/axiom lists include the new module and
retain every old entry. No workflow, pin, audit or allowance is weakened.

This is the same cut and the same bound/tolerance/payoff/fallback, not a rate
for changing configurations or a proof of independent later-PBS re-solving.
Native first-exit bounds and CarriedResolveStepBounds remain open. All four
M06 parent coverage rows remain pending; M06 is NOT complete. Keep finite-T
error at zero oracle error and the printed/corrected Theorem3 distinction.
