# M06 execution-conditioned native queries

## Restart checkpoint

Work branch: `rebel/m06-conditioned-query-20260925`.
Parent source: `3cfc5e0cc27b2574f585a8a8763794ba73d05687` on
`rebel/m06-joint-query-repair-20260925`. Preserve all preceding checkpoints.
Main remains the accepted M05 source `6a6cbdaa8b4fb43b35a9d3e555a7c1a614130098`.
M06 and Theorem 3 remain incomplete.

The parent's actual targeted run `36149297405`, job `108118391960`, was
inspected: the declared M06 targets compiled successfully, including the
repaired joint-query controls. Its targeted normal/slow lint and axiom stage
was still running. All-ReBeL `36149297460` and full CI `36149297382` must
still be read before accepting that parent. Compilation is not axiom evidence.
The exact source was downloaded through the GitHub plugin as artifact
`10870801883`; its embedded source SHA equals the parent. Local inspection
uses only this source export, not local Git or direct GitHub network access.

## Next dependency-closed slice

Construct a tagged native execution: draw the private CFR iteration and root
type independently from their actual model laws, then run the legal own
iterate against fixed opponents from that type's compatible conditional
history law. Retain the seed/type tag alongside the resulting history.
Condition this actual execution on a positive-mass event, which may correlate
the seed and type. Derive, rather than assume, the resulting query's density
or domination by the original product law. The expected nonnegative native
gap should cost at most the original finite-T bound divided by the ACTUAL
event probability. A mass-weighted statement must preserve the rare-event
cost; no uniform lower event probability is implied.

Use existing FinDist bind/map/condOn and canonical full-information execution.
An impossible event must not produce a fabricated normalized posterior.
Supply a live canonical solver control and a correlated selection control
showing why an unconditioned mean bound cannot simply be reused.

This is a fixed-slice, fixed-average-opponent native gap result, not a theorem
about arbitrary independently changed PBS kernels or recursive re-solving.
The original SEARCH-FRONTIER, SEARCH-CFRD, SEARCH-ERROR and SAFE-THEOREM3
obligations remain pending. First-exit rates and CarriedResolveStepBounds
remain explicit future obligations. No proof or coverage acceptance is claimed
at this planning checkpoint.
