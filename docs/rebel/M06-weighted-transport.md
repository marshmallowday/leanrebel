# M06 actual-prefix-weighted transport and root security

## Source obligations and scope

Continue SEARCH-ERROR and SAFE-THEOREM3: main section 5.1 and Theorem 3,
supplement G pp.21-22. Preserve the original printed/corrected distinction.
This slice is a fixed-cut root-security improvement, not independent re-solving
at every later carried PBS. No M06 parent status is promoted.

## Canonical execution and computed cost

cfrDWeightedEnvelopeLoss integrates a query-dependent cost under the actual
privateCarriedPrefix, with the selected iteration and its reached history kept
joint. The model reference conditional is not substituted for this actual law.
Perfect recall and unilateralReference_density derive the necessary change of
measure against any fixed unknown behavioral opponent. There is no worst-case
inverse reach or density multiplier.

For a live query the concrete cfrDFreshTransportCharge is

    newChildLoss + max(0, freshValueChange) + 2*payoffBound*conditionalTransportDefect.

Stopped queries cost zero. conditionalTransportDefect is the already derived
conditional atom L1 cost on jointly supported queries; a disappearing NEW query
costs one without consulting a fabricated NEW posterior. No reference equality,
information-local density between old and new models, or support inclusion
between the two model laws is assumed. NEW local child quality is an explicit
ordinary solver premise at the generic boundary, not a final safety certificate.

privateCarriedResolve_weighted_envelope_le_prefix integrates the local bounds.
cfrDDepth_fresh_weighted_security uses actual parent focal and prefix regrets
and the derived fresh query bounds. Its original-game allowance retains

    C_error*predictionError + C_time/sqrt(T) + oldChildLoss
      + actualPrefixExpectation(newChildLoss + positiveValueChange + 2*B*transport).

The expectation is live-only and retains private seed/history correlation.
No claim is made that each individual CFR iterate is Nash. The private resolver
is the existing final-average legal-plan draw, not the native-iteration sampler.

## Controls and exact-source validation

The two new modules are registered in the analytic root, every M06 target and
the supplemental normal/slow lint and transitive public/private/generated axiom
consumer. All prior 126 targets and 88 supplemental modules are retained, making
128 and 90 respectively. No workflow, dependency pin, warning policy or axiom
whitelist changes. Local ledger/inventory checks pass but are not Lean evidence.
Exact-SHA compiler/lint/axiom validation is PENDING at this source checkpoint.

Controls include an actual twice-recomputed hidden-type child with losses 1/4
and 1/8, arbitrary unknown opponents, zero execution fuel, a genuine conditional
transport of two visited with probability 1/100 (weighted mean 1/50), and a
negative guard against replacing joint private seed/query law by independent
marginals. These finite-law guards are not source-Theorem3 counterexamples.

## Remaining obligations

Useful rate bounds on the actual weighted cost, further original-game solver
consumers, later carried-PBS independent re-solving and CarriedResolveStepBounds
remain. A computed expectation alone is not a source-level vanishing rate.
Never identify model PBS with the actual unknown-opponent posterior, infer
learned-network accuracy, or remove finite-T and positive numerical-error terms.
