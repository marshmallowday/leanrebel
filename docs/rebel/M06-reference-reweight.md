# M06 information-local reference reweighting

Source obligations: main section 5.1 and Theorem 3, supplement G pp.21-22;
retain the printed/corrected distinction in M06-fresh-chain.md and the ledger.
This is a restricted transport lemma, not full original Theorem 3 acceptance.

## Exact boundary

For OLD reference A, NEW reference B, and the complete information/live tag q,
assume A.prob h = B.prob h * weight(q h). The direction ensures every OLD
queried fiber remains supported under B. The established canonical
informationReweight_conditional theorem then gives equality of queried
conditionals, without equality of full laws or observation marginals.

CFRDReferenceReweight derives signed-change composition, positive-maximum
subadditivity, its finite parent-family version, and the coherent resolver
envelope from that density. The envelope retains local child loss and measured
value drift. Local child quality is a premise of this generic theorem, not
a supplied final safety certificate. Arbitrary independent later solves need
not satisfy the density; where it fails, posterior transport and support
loss must be charged rather than identifying model and actual posteriors.

## Controls

Examples.CFRDReferenceReweight uses correlated four-point laws with different
public masses but the same nondegenerate hidden conditionals. It checks the
atomwise density, support, genuinely unequal laws, arbitrary observables and
signed composition. Opposite supports and same-observation/different-hidden
laws reject invalid density assumptions. These are finite-law guards, not
source-Theorem-3 counterexamples. The final canonical hidden-type control uses
the existing actual two-solve chain, with losses 1/4 then 1/8; its already
derived reference preservation supplies the density-one specialization.

## Validation state and registration

The recovered patch's five code/registration blobs match the previously
unapplied archive exactly. All 124 old M06 targets and 86 supplemental audit
modules are retained, adding two to each list. All prior controls, pins,
warning gates, heartbeats and axiom whitelist are unchanged.
This implementation checkpoint has not yet received exact-SHA Lean acceptance.
Run the existing M06 targeted, full CI and independent ReBeL gates and record
results in M06-reference-reweight-validation.md. Fraction checks and source
hashes do not replace compiler, lint or transitive public/private axiom checks.

SEARCH-FRONTIER, SEARCH-CFRD, SEARCH-ERROR and SAFE-THEOREM3 stay pending.
Remaining: useful value-drift/support/first-exit rates and the actual later
independent solves' CarriedResolveStepBounds. M05 evidence remains untouched.
