# M06 structurally recursive PBS CFR-D — candidate

Work branch: `rebel/m06-recursive-solver-20260924`, from repair source
751ad17af36d6ff4f3deea2c1ee513f10dc754d9. Parent checkpoint:
a01832184554acc81da55d991f118654ef5c5442. The old structural-recursion branch
is preserved so its repair validation is not cancelled by this implementation.
Main is unchanged and not a write target. All repository operations use the
GitHub plugin. See M06-structural-recovery.md for the precise inherited failure.

## Parent combinator

PBSComposedDepth uses the actual coupled parent recurrence with a computational
PBSChildSolve. Each current trunk constructs its factual joint PBS; the child
receives the joint mass-floor-scaled positive target. Public splicing and
counterfactual completion supply every live query, including zero-own-reach
queries. Numerical perturbation is applied to that same parent's values.

cfrDComposedOracle_accurate and cfrDComposedOracle_leafOptimal feed the existing
finite-T Nash theorem. pbsComposedDepthProfile computes a nonempty finite count
AFTER accounting for numerical and child errors, and transfers the rooted
all-deviation guarantee to the original PBS. Its chance-root translation retains
one administrative transition separately from the original cut length.

## Actual structural consumer

PBSRecursiveDepth takes a finite list of original cut lengths. Its definition
recurses on the strict list tail, allowing the protocol and information model
to change to the newly constructed canonical chance-root adapter each time.
Uniform carrier universes are explicit; this includes the ordinary finite games.
The empty list returns the legal fallback and executes zero original transitions.
It does not delegate to a full-root CFR or accept an equilibrium witness.

pbsRecursiveDepth_isNash uses list induction. In the step, the induction
hypothesis on the newly rooted model discharges PBSChildSolveAccurate for every
factual child belief and every positive mass-scaled requested target. The main
statement retains finite histories/actions, zero-sum bounded payoffs, positive
target and an explicit bounded numerical-noise family. It has no supplied child
Nash, recursive-accuracy or local-regret certificate. Every parent computes its
positive finite round budget using the existing allocation. Fixed unallocated
prediction error is not made small simply by increasing T.

pbsRecursiveAllocatedNoise is a concrete nonzero allowance-sized perturbation,
with its bound proved at every level. It demonstrates that the proof does not
silently set errors to zero; it is NOT an arbitrary learned predictor's accuracy
claim. All these definitions remain a noncomputable real-valued specification,
not an executable floating-point refinement.

## Controls and evidence discipline

Examples.PBSRecursiveDepth uses the canonical hidden-type game's initial PBS,
its actual chance transition and both real decision stages. Controls cover
three recursive levels, a fresh randomized deviation, hidden-information
locality, empty schedules, a zero-width cut, positive numerical bias, and
zero-own-reach leaf completion supplied by the derived recursive theorem.

All previous targets, analytic imports and supplemental lint/axiom entries are
preserved; the three new modules are appended to all three. These are compiler
candidates until exact-source CI and transitive audits are inspected. No source
parent is promoted to verified just by adding a candidate theorem.

Coverage parents SEARCH-FRONTIER, SEARCH-CFRD, SEARCH-ERROR and SAFE-THEOREM3
remain pending. Source-level drift, useful support/first-exit rates and independent
re-solving safety remain separate original obligations. Private carried play
must not be identified with arbitrary independent repeated re-solving.
