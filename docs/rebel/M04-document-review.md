# M04: complete document reread and chronological locality checkpoint

## Original request and current execution policy

The original request was reread in full: read every docs/rebel file, then
complete M04; preserve focused remote checkpoints; all GitHub access and writes
through the plugin; do not stall on permission assumptions; repair defective
premises while continuing; avoid worktrees. The latest user message explicitly
reauthorizes Actions after making the repository public. The older no-Actions
instruction is historical, not the current acceptance policy.

## Complete reread, not a download-only claim

All 30 files recursively under docs/rebel at
507f81dd84e6ef94b4d275581fc3f86a886b30fa were read, including historical logs,
all original and overridden coverage rows, the complete paper inventory,
licenses, source/version hashes and candidate-reuse assumptions.

Long exact text was read through GitHub job logs of review run 35305351496.
Jobs 105476303548, 105476303514, 105476303591, 105476303522, 105476303564,
105476303579 cover all six contiguous ranges of a 206168-character stream.
The ranges are [0,34361), [34361,68722), [68722,103084), [103084,137445),
[137445,171806), [171806,206168). Each includes the same complete file manifest.
They supplement the complete direct reads of the other Markdown and log files.
The temporary review workflow is read-only and is not a proof acceptance gate.

The compressed official inventory was read in full through existing exact
expanded-output jobs 105038965298, 105038965015, 105038965256, 105038965152.
Their four contiguous ranges cover all 186257 expanded characters. The current
manifest confirms unchanged blob b86760a73c5697be6ea408eb5f774fcab13fb04a and
SHA-256 802333959487fac1624c27d387d4a33cf778e15f739863591071f506ae7e7c2b.
The current ledger SHA-256 is
46233627cb88ae750411948ee60eb7c8037f6eb511b112e321874e39ca835d64.
No row is removed or promoted merely because it was read.

## Actual new proof work

ChronologicalLocality.lean derives continuation-law and counterfactual-regret
invariance under own-policy changes at earlier and distinct equal-depth sites.
It uses the actual canonical runner and an information-local observation clock.
Its arbitrary-law root identity discharges common depth from the clock and
common own reach from perfect recall; it does not assume a desired root gain.
The full two-stage hidden-type example exercises distinct private-information
sites at the same depth against arbitrary behavioral profiles and laws.

This checkpoint has source proofs but is pending its own Actions validation.
It is not the finished root sum, coupled CFR trace, averaging/Nash theorem or
rational refinement. Those M04 obligations remain pending. Existing coverage
and M03 qualified boundaries are preserved. Resume from the actual branch HEAD
and its compile/lint/axiom results, not from the existence of these filenames.
