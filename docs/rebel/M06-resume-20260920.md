# M06 isolated resume: inherited compiler repairs

This work continues on `rebel/m06-resume-20260920`, forked from the actual
`rebel/m06-recovery` head `6a43cb5c6899548093dd8e9b2590e6996976d853`.
Recovery moved while it was being reviewed; its newer width repair is retained.
Main remains the accepted M05 source `6a6cbdaa8b4fb43b35a9d3e555a7c1a614130098`.
No concurrent recovery change is overwritten and no history is rewritten.

The baseline targeted run `35477960893`, job `105990401820`, at
`e0242aa19ad5a1c59a7f9d227249dc66b4b14e4f` failed on actual Lean errors:

- CFRDZeroReachOptimality: insufficient type information for an expectation
  congruence. Give the distribution the explicit type `FinDist E.History`.
- CFRDZeroReachControl: an injectivity simplification did not rewrite the
  conditional. Split equality and use the equivalence's injectivity directly.
- CFRDZeroReachControl: unfold the named history in the zero-reach premise
  before rewriting at the explicit decoded history.

This checkpoint keeps every theorem statement and every positive/negative
control. No warning suppression, axiom, placeholder, relaxed audit, or target
removal is used. Exact-source compiler and full acceptance checks are pending.

Next connect supported-type Nash optimality to the constructed focal
zero-own-reach completion. The existing PBSLeafOptimality already proves
optimality for supported types; it must be reused, not counted twice.
Root-law preservation alone is not conditional continuation preservation.
Joint off-path completion, recursive child quality and the full Theorem 3
connection remain open. M06 is not accepted by this compiler-repair checkpoint.
