# M06 completed reference-query sampling

## Restart and source scope

Continue on `rebel/m06-completed-query-sampling-20260921`, descending from
`a1aac9c6890ed712987dccbcc83bcf6910a404d7` on the factual-query branch.
The latter fixes the newly exposed unused finite-root instances in
`cfrDInformationQuery_support` without disabling the warning. Targeted run
`35580639780`, job `106272457481`, records that exact predecessor failure at
`5270cb63bf0d0de9ae9ae20b0a6111732a69ca8d`.

The new module `CFRDInformationQuerySampling` is registered in the public
analytic umbrella and M06 target list. The existing recursive module discovery
includes its private helpers and public declarations in transitive-axiom and
complete normal/slow-lint checks. This is candidate source until its exact
commit's checks are inspected.

## Construction

A supported factual query uses the actual retained child-iteration family with
its original public-posterior-dependent count. Its complete conditional law is
unchanged by zero-own-reach completion, even against arbitrary fixed opponents.
A genuine reference query absent from factual play instead uses the existing
computed public response completion. Its zero OWN reach follows from canonical
reference support; zero joint probability alone is never used as a substitute.
No absent public posterior is fabricated, and no zero-probability cancellation
or per-iterate Nash claim is made.

`cfrDInformationQuerySample_law` joins those branches to the existing
`cfrDInformationContinuation`. `cfrDInformationQuerySample_value` preserves every
history observable. `cfrDInformationQuerySample_gain_le` consumes the existing
derived leaf contract for the same sampled law and every full future deviation,
retaining the positive finite child loss. Neither a leaf-optimality certificate
nor a root-equilibrium certificate is a new caller input.

## Remaining scope

Validate this exact source and add explicit factual-zero, reference-positive
hidden-type controls. Connect the sampled value interface to the actual noisy
parent recurrence. Then prove independently re-solved recursive carried-PBS play
against a fixed unknown opponent with explicit finite outer-T, prediction and
child losses. The one-query laws are not that recursive theorem.
Original `SEARCH-FRONTIER`, `SEARCH-CFRD`, `SEARCH-ERROR`, `SAFE-THEOREM3` coverage
remains pending. Keep the printed and corrected finite-T Theorem 3 distinct.
