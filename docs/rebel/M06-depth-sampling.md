# M06: native depth-limited sampling through carried execution

## Preserved resume source

The implementation starts at `964f6d639236d11addf44b79b3dee3fc2ef5d67b`
on `rebel/m06-rooted-depth-20260924`. Its remote M06 targeted proof run
`35888559368`, job `107274927401`, completed successfully, including both
`Compile the declared M06 targets` and `Validate the constructed exact child
proof slice`. ReBeL checks `35888559003` and source inventory `35888559143`
also report success. The older STATUS wording that these checks were pending
must not be used to repeat the already completed implementation.

## Active checkpoint

Continue from the actual HEAD of `rebel/m06-depth-sampling-20260924`.
The next dependency-closed slice is to lift the noisy depth-limited parent's
actual-iteration law from its model root to each supported original history,
then to the full native profile/PBS state and the existing finite schedule
runner. The root distribution and private iteration law are not identified
with the unknown opponent's actual conditional law.

## Acceptance boundary

Implementation and exact-source validation of this new slice are pending at
this checkpoint. Add concrete positive and boundary controls and connect all
new Lean modules to the analytic root, target list, and normal/slow lint plus
transitive axiom auditor before accepting it.

This is not a claim of arbitrary-depth recursive child solving or of a
source-derived vanishing exceptional-mass rate. Model-value drift,
CarriedResolveStepBounds, and the unrestricted corrected Theorem 3 remain
separate obligations. Retain finite T, positive child loss, prediction error,
zero-reach completion, and the printed/corrected theorem distinction.
SEARCH-FRONTIER, SEARCH-CFRD, SEARCH-ERROR, and SAFE-THEOREM3 remain pending.
