# M06 public-observation posterior bridge

## Starting checkpoint and scope

Continue on `rebel/m06-conditioned-query-20260925` from
`543849ea0fc3df25c95629d957561936ee16527a`, not main. Branch enumeration,
the latest repository-wide push workflows (which have no branch filter), the
source artifact's recorded SHA, STATUS and a fresh branch-ref read agree on
that starting checkpoint. The source artifact 10896456006 has SHA256
38b5471a57e581fb2e1000ed3de7c479ca49ac44fe53463e8440e1ae235a98e1.
Main remains accepted M05. M06 and Theorem 3 are incomplete.

This checkpoint records a dependency-closed next slice, not a completed proof:

1. Prove, through the public FinDist API, that conditioning on the preimage
   of an observed event commutes with projecting the original law. Require a
   real supported witness on both sides; do not manufacture an off-path law.
2. Apply it to the actual retained-seed/type/history CFR execution and a public
   trace event. Identify its selected HISTORY marginal with the canonical
   PublicBelief.condition of the computed-average execution. Derive equality
   of the public event masses and possible-observation witnesses.
3. Connect the existing native conditional mean estimate to that actual public
   event probability, retaining its reciprocal and the finite-T residual.
4. Add a genuine solver consumer, finite-law positive and boundary controls,
   and include all new modules in compiler, lint and transitive-axiom gates.

## Semantic boundary

The execution uses arbitrary fixed legal opponents. The gap is still measured
against the SAME computed average comparison opponent at the ORIGINAL full
joint type kernels. Projected history equality is not independence of the
posterior tags and is not equality to a fresh independently re-solved PBS.
Only events observable from the projected public trace get this posterior
identification: a private seed/type selection is not a public update.

Changing-opponent/PBS rates, quantitative first-exit/support rates, recursive
re-solving safety and CarriedResolveStepBounds remain open. No source ledger
row is promoted: SEARCH-FRONTIER, SEARCH-CFRD, SEARCH-ERROR and SAFE-THEOREM3
remain pending. M07 is not started.

## Validation state

Planning checkpoint only. The previous conditioned-query proof repair has its
separate exact-SHA validation in M06-conditioned-query-repair.md. New proof
source, exact-SHA compiler, normal/slow lint and transitive-axiom evidence must
be recorded here before this slice is called validated. GitHub access and
commits use the GitHub plugin; offline snapshot inspection uses no local Git
operations and no direct GitHub HTTP.
