# M06 approximate child continuation: probability budgets

Proof source: af8b63282a49cdfb68df411e5527b6ba3e288d42.
Restart: rebel/m06-conditional-checkpoint-20260920, actual remote HEAD.
Scope: ROADMAP M06, SEARCH-ERROR/SEARCH-CFRD dependency, not acceptance.
Exact-source compiler/lint/axiom evidence and semantic review are recorded in
M06-conditional-loss-validation.md. All eight general theorems and eight
example theorems compiled; the supplemental 13-module validation passed.
Full workflow states remain separately recorded in STATUS.md.

PBSApproximateOptimality derives mean and probability-weighted conditional
best-response gaps from canonical approximate Nash and a single legal
remembered-type response. For positive type probability p the bound is
epsilon/p, not epsilon. A root budget epsilon <= p*loss yields conditional
loss. Zero-probability types are not certified by the weighted inequality.
Its fifth theorem, finiteBeliefForm_approxNash_realization, transfers actual
mixed-plan approximate Nash to the canonical behavioral PBS game at the
same error, using complete unilateral laws and finite predrawing.

CFRDApproximateLeaf connects the budgets to joint zero-own-reach completion,
legal public-response splicing and the constructed reference table. Factual
queries use probability-budgeted approximate PBS Nash. Nonfactual queries
use the existing computed conditional best response at zero own reach.
The result is the actual CFRDLeafOptimal contract at the stated loss;
query existence, kernel equality and public-state agreement are constructed,
not extra caller certificates. The approximate child Nash and budget premises
still require an actual finite-iteration child solver.

Examples/CFRDApproximateLeaf reuses the canonical two-player zero-sum
finite type-plan matrix game. Root error p coexists with conditional gain one
for arbitrarily small positive p; at p=0 even exact root Nash leaves that gain
unconstrained. All mixed complete-plan deviations are covered. A separate
live HiddenTypes example uses the actual constructed reference table and
its existing exact child as a zero-error specialization, not as a claimed
nonzero-error finite-child-CFR implementation.

All three modules are registered in the analytic umbrella, 51 distinct
M06 targets, and the supplemental complete declaration-axiom/normal/slow-lint
consumer. Every inherited real target and verification gate is retained.
Earlier failed compiler attempts and their exact repairs are preserved in
the validation document rather than omitted from the development record.

Coverage journal: SEARCH-ERROR gains a probability-aware conditional-loss
bridge, and SEARCH-CFRD gains a sufficient approximate child-game input to
the actual leaf contract. SAFE-THEOREM3 still needs the finite child solver,
its budgets and fresh recursive carried execution. All original parent rows
and inherited exact-driver evidence remain unchanged; no milestone is closed.
