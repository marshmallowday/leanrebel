# M06 recovery checkpoint

The initial remote read found `rebel/m06` at
`9c1625ee92bc25ed33914b58a641403739825e67`. Its exact-source artifact was
retrieved through the GitHub connector and its recorded TAR SHA256 was checked:
`b5287f1358554442280d7d27272705b0969b82e10d48e2cdb74cf7b28b1ce65c`.

Before writing, the remote ref was read again. It had advanced through
`c25f0e903c7db3733d1553727e1ceb43fce37b6c` (three scalar proof repairs) to
`f179d3ff2956b5798825c642cb05c6d6de8ed8e0` (compiler annotations).
The recovery branch `rebel/m06-recovery` starts at that latest commit so the
additional changes are preserved and concurrent work is not overwritten.
Main was last observed at `6a6cbdaa8b4fb43b35a9d3e555a7c1a614130098`.

## Observed validation, not milestone acceptance

- `9c1625ee`: inventory run `35454311005` passed.
- ReBeL run `35454310991` failed before compilation on two over-width lines.
- Target run `35454311022` compiled Frontier, ConditionalOracle and CFRDTrace,
  but CFRDRegret had three scalar proof errors. The inherited `c25f0e90`
  addresses precisely those errors; its result is not presumed here.
- Full run `35454311038` was cancelled and is not acceptance evidence.

## Work remaining

Repair the line-width gate, validate the inherited scalar repairs, then derive
counterfactual leaf-vector backup and off-path continuation adapters for the
actual coupled solver. Finish explicit finite-iteration error constants,
private random-iteration execution with carried model beliefs, unilateral
safety versus arbitrary opponents, and adversarial seed/averaging controls.
Keep the printed and corrected Theorem 3 statements separate. No oracle data
field may simply assume the final root-regret or safety conclusion.

The document review is being renewed from the exact source; the earlier
baseline review is recorded in M06-checkpoint.md. This checkpoint does not
claim that the renewed review or M06 acceptance is complete. Continue on the
recovery branch and inspect its latest SHA and CI first. All remote writes use
the GitHub connector; no force update or local GitHub connection is used.
