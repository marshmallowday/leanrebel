# M04 coupled-trace development checkpoint

## Source-linked validation and corrected provenance

Source 1a8e5b6e5e8db19cd87b2779604c4384ee10feec has a successful ReBeL
compiler/lint/axiom job 105481483330. The check-runs API and artifact metadata
were re-read to resolve a mistaken transcription of its run/artifact numbers.
The correct run is 35307119886, artifact 10532285794, archive SHA-256
0d8acd2971a56b73bf7390d67f72f24fcc7ef40af89cc63579514e14b01b8ca9.
These replace the incorrectly transcribed identifiers in the previous version
of this checkpoint. That version's numerical module/declaration counts are
not used as acceptance evidence. The canonical log and its source SHA control.

The distinct full-CI run is 35307119694, job 105481535317. It was cancelled
when the later checkpoint started, not accepted as a successful full build.
The exhaustive root identity and full-game examples remain in the actual
compiler/lint/axiom consumer; no gate or theorem has been relaxed.

## Current unaccepted source slice

RegretMatching supplies a real positive-regret rule with an explicit legal
fallback at zero positive mass. CFRTrace builds one simultaneous whole-game
profile sequence: all local tables read the same previous-round snapshot.
Round zero has no observations and uses the specified fallback, not a warm
start. Local realization, average-vector equality and cumulative-regret
identities are tied to that actual shared play sequence.

Run 35307848156, job 105483611765 on ee2ff41b690103279679cae897dd0386db294a04
stopped at the style requirement to use let rather than letI inside a proof.
This checkpoint makes that exact correction; the mathematical statement and
proof steps are unchanged. PayoffBounds derives unit-interval reach bounds,
finite-fiber counterfactual payoff bounds and the actual Euclidean norm bound
from the game's absolute payoff bound. Those new sources still need their own
compiler, full lint and transitive-axiom results.

Remaining work: connect the coupled tables to all deviations through the
constructed root identity; own-reach averages and independent private seeds;
canonical Nash; executable rational refinement and independent best-response
tests. No coverage row is promoted. All remote operations use the GitHub plugin.
Every new module is imported by the analytic root and included in the recursive
proof consumer. Final acceptance requires actual same-source validation.
