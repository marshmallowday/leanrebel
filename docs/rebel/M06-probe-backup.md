# M06 checkpoint: canonical deviation and counterfactual backup bridges

Continue on `rebel/m06-recovery`, preserving concurrent work on `rebel/m06`.
The preceding saved checkpoint is `437b073766459fc75b4f856226227ddf860e6607`.

## Proved and locally compiled

- Information-local reweighting preserves the actual posterior conditional
  law on every positive-mass observation; zero-mass fallbacks are not equated.
- `DominatingReach` constructs uniform legal own play with positive own reach
  on every legal trace. The opponents and chance remain the actual iterate's.
  Perfect recall and the terminal-aware canonical runner prove the density
  for EVERY unilateral alternative. A single reference value vector therefore
  covers deviating laws, including zero-factual-own-reach branches.
- `CFRDProbe` constructs strictly earlier own-policy probes. Their action
  regrets equal the original canonical counterfactual regrets; their reach is
  a positive game/menu-dependent quantity independent of the iterate.
- A proved single-site root identity shows that the normalized full scores
  differ from counterfactual regrets by one action-independent offset. The
  actual current local law cancels that offset. Prefix information-vector
  error transfers to each normalized score with an explicit inverse uniform
  own-reach factor. Full-game rollout is used only in the correctness proof.

The new modules and the expanded reweighting module compile with pinned Lean
4.33.1, warnings treated as errors. They are in the umbrella/target lists and
are automatically included in the recursive full audit. Actions acceptance
must be read for the resulting exact commit, not presumed from local checks.

## Remaining

Integrate terminal-exact/live-cut-only oracle reads into the coupled driver;
discharge the numerical regret and off-trunk continuation contracts; derive
finite-time constants and the actual private random-iteration safety bridge;
finish carried model beliefs, recursive replacement and negative controls.
This is an intermediate checkpoint, not M06 acceptance or a coverage promotion.
