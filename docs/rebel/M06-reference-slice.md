# M06 actual reference-query table construction

Work branch: `rebel/m06-reference-table-20260920`.
Parent implementation: c09ff8b50eaa9cc3949aadbb43c46063b907c1e1.
The parent public-splice branch is preserved so its CI can finish separately.
This new code requires its own exact-source verification.

CFRDReferenceSlice constructs the typed table previously assumed by the public
response interface. Its types are all physically attained full root AOHs;
publicRootMemory provides persistence. At every sampled live type, the kernel
is exactly the original unilateral reference law conditioned on that full
information state and live flag. Its public fiber and private type are proved
from the actual conditional support and public reconstruction, not assumed.

Unsampled kernels use the existing physically compatible witness completion.
This is not called a posterior. A physically impossible public history has
no table entry at all, and cfrDReferenceTable_empty proves this for [] because
the initial public observation is retained. cfrDReferenceTable_query constructs
a matching entry and exact kernel for every sampled live query.

cfrDReferenceTable_leafOptimal connects this actual table to the constructed
public best responses and zero-own-reach completion. It no longer requires
query existence, typing, kernel equality, or local policy agreement from the
caller. Factual child-game Nash remains an explicit premise for the given base
profile. It is NOT claimed for an arbitrary trunk or an arbitrary finite-T child.

The new concrete HiddenTypes query control uses an actually reference-supported
history at an entirely unvisited factual public state. It obtains its typed
table and exact joint conditional by invoking the constructor, with no supplied
kernel certificate. The prior public-splice and zero-reach controls remain.

Next construct the factual child Nash responses (or the separately quantified
finite-T approximate counterpart), connect actual recursive child execution,
and propagate child loss through the depth-limited security theorem. The
original M06 parent ledger obligations and Theorem 3 acceptance remain open.
