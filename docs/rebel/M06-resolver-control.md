# M06 public resolver control checkpoint

The generic public resolver transfer and its finite-time security connection
compile at `04cf850a16e7cf9a56c3b97f66655f285e88a1d0`: targeted Actions run
`35471149309`, job `105972256452`, succeeded. This is targeted compiler evidence,
not full lint/architecture/axiom acceptance.

The full static gate detected a new `change` tactic in CFRDResolve, counted
by the inherited Analysis transport budget. The tactic was unnecessary:
the existing exact proof checks the definitionally equal target directly.
It is removed here; the audit definition and its expected count are unchanged.

The new CFRDResolveControl uses the actual depth-two, one-remaining-step
hidden-type CFR-D driver and the fair continuation. It derives the local
replacement contract from equality of canonical second-round laws against
EVERY unknown opposing behavioral policy. The public resolver sees only its
allowed query data, and the actual opponent is never replaced by model play.
Terminal absorption and zero-model-probability branches are retained.
Its own exact-SHA compiler result must still be checked.

This is not an acceptance of unrestricted recursive re-solving. The generic
CFRDResolverLocal premise remains a real continuation obligation; the example
supplies it, while a general recursively computed PBS solver still needs its
own construction and proof. Preserve numerical oracle error, continuation
optimality loss, replacement loss and finite-iteration error as distinct terms.
M06 remains in progress; main and accepted M00-M05 evidence are unchanged.
