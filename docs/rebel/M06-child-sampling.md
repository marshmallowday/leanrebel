# M06 constructed child-sampling bridge

Restart branch: `rebel/m06-recovery`. The preserved parent checkpoint is
`883f33291a5898ffd60e626f77e01497ea906960`. Its target run `35474386801`,
full repository CI `35474386837`, and inventory `35474386809` were observed
successful. The separate ReBeL run `35474386803` was still running at the
initial read. Main remains at the accepted M00-M05 checkpoint.

The previous complete document-review records are retained, not relabeled
as a new complete rereading. The GitHub comparison from `7eec0533` to this
parent shows nine added M06 documents and a changed STATUS; all ten were
read, together with the relevant canonical execution and averaging proofs.
The original paper's Appendix G and its separate finite-time term were
rechecked. Existing corrected/source distinctions and negative controls stay.

## New proof path

Do not infer pointwise replacement safety from arbitrary Nash selections.
Instead construct the parent's continuation witness from the very child
policies that will be sampled. CFRDDelayedSampling clamps every child to the
selected trunk and applies the existing independent own-reach average.
A private draw delayed until the cut has exactly the same complete history
law as that virtual averaged continuation against every fixed opponent.
The proof retains early terminals, zero factual own reach, and actual chance.
No Nash, payoff bound, supplied root regret, or local no-loss premise is used.

This is a compiler checkpoint, not acceptance. The new module is registered
in both the explicit analytic umbrella and the M06 compiler targets. No gate
or existing test is removed. Continue with the adapter to the actual coupled
value oracle and carried resolver, then explicit child-solver contracts and
full exact-source lint/architecture/runtime/axiom validation. M06 is open.
