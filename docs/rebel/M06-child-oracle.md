# M06 same-iteration child-oracle adapter

Continue on `rebel/m06-recovery`. The delayed-sampling repair `b717496c`
passed targeted run `35476164384`, job `105985660828`. Subsequent code must
be validated at its own SHA; that successful parent is not acceptance of
this adapter. The preceding child-resolver checkpoint is `5aab99e2`.

CFRDChildOracle constructs the parent's virtual continuation and numerical
value response from one child table. cfrDChildTrunk reads the actual coupled
CFR-D state; cfrDChildQuery uses exactly that trunk and round. The actual
carried public resolver samples precisely this query's child policies.
Its law equals the parent private-iteration mixture for every fixed unknown
opponent. Thus the existing finite-time security bound transfers without
CFRDResolverLocal, arbitrary-equilibrium replacement, or an extra loss term.

The exact-reference builder derives numerical value accuracy. It does not
prove that arbitrary child policies are optimal or computationally cheap.
The security theorem keeps the local continuation-quality premise distinct
from prediction error, and keeps the finite-iteration term when error is zero.
The table is fixed by the parent query; it is not yet a full implementation
of a fresh recursive solve for every realized carried PBS.

Important remaining boundary: own-reach averaging may use its stated fallback
at zero-reach information sets. Law equality on actual execution does not
prove all counterfactual continuation contracts. Construct and verify the
recursive child solvers and their compatible off-path completions; do not
infer that obligation from the sampled-law equality or ordinary on-path Nash.

Both new modules are registered in the analytic root and targeted compiler
list, and remain covered by the recursive lint/axiom audit. Compiler feedback,
canonical positive and negative controls, and exact-source full validation
must be inspected before acceptance. M06 and its coverage remain open.
