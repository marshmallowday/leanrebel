# M06 constructed optimal zero-own-reach response

The source checkpoint `ef5b781639ffa7180d87805061c0781eb725c6ac` introduces
CFRDZeroReachOptimality. Its compiler and full acceptance must be checked
at the new exact source, rather than presumed from earlier module success.

Zero own reach is proved to persist along every legal continuation. At such
a root, completing a focal policy therefore gives the complete continuation
law of the selected completion against fixed opponents. The implementation
then uses the existing finite best-response-plan maximization and legal
remembered-type splicing from PBSInfoValue to CONSTRUCT the completion.

One information-local response attains every compatible conditional kernel's
best-response value when its root histories have zero original own reach.
It is optimal against every complete behavioral future deviation, not merely
a finite action list. Its whole original-game law remains unchanged against
every fixed opposing policy. Optimality is derived from canonical finite-plan
attainment, not an oracle structure field containing the desired inequality.

The remaining premise is structural: the selected kernel's root support has
zero own reach. It is not zero joint reach and not zero current PBS mass.
The new HiddenTypes control explicitly distinguishes these cases. This focal
construction fixes the other players; joint completion, the supported-type
Nash bridge, and recursively computed child tables must still be connected
before claiming the general M06 continuation contract or Theorem 3.

The source is registered in the public analytic root and declared M06 targets,
with no placeholder or relaxed audit. M06 remains in progress. Review source
and exact-SHA checks before deciding which obligations are actually closed.
