# M04 recovery and rational frontend — 2026-09-18

## Read and resume boundary

The remote branch was checked at `c9c6d67e2e6b0fba0d251a8c42b7dc18a6316638`.
The prior response's checkpoint was actually pushed. main remains the accepted
M03 commit `cf733bd1ffa977681d1197bf80b1dfd7be82ff6e`.
GitHub reads, writes, commits and reference updates use the GitHub plugin;
compilation uses the existing Actions workflows with the pinned dependencies.

The complete original first chat prompt is not present in the current context.
Personal-context retrieval recovered its task and constraints, but only a
partial quotation/summary, not an independently verified verbatim full prompt.
Do not describe that as having reread the complete original. The current explicit
user instructions, both AGENTS files, ROADMAP's M04 acceptance conditions and
persisted development/review records govern this continuation. The older full
docs read is recorded in M04-document-review.md, not claimed as a new full read.

## Observed compiler progress

- Arrival `c9c6d67`: IndependentRealization and UnilateralAverage compiled;
  AverageNash failed on a nonexistent expectation-negation lemma and an
  underdetermined canonical Nash characterization application.
- `16d18ce184bfab29b4a37faff9fa8956f6a20e3c`: AverageNash compiles in ReBeL
  run 35340209806/job 105584142349; CFRNash failed when inferring the real
  observable in the completed-round expectation rewrite.
- `d2be3304e738574d3d8097f5ce5c1c9b655002c3`: that observable is explicit.
- `83d585b028fb6606f5bcb3c9d11ab7eb35b976f5`: concrete hidden-type CFR theorem
  added. The proof-surface regression correctly rejected its missing umbrella
  import. This checkpoint repairs the import without weakening that test.

This checkpoint adds executable rational regret matching, an explicit pure
fallback, weighted-policy normalization and average-regret arithmetic, together
with exact cast proofs into the existing real matcher. This is not yet the
full-game rational evaluator/iteration refinement. Its CI must be checked on
the resulting commit before any verified claim is made.

## Next work

1. Finish the rational full-history evaluation and simultaneous-iteration
   correspondence, using actual chance and continuation values, not a regret oracle.
2. Execute the two-stage case and independently enumerate all information-local
   best responses (including own-action memory), with zero/one-round and
   wrong-averaging/incorrect-coupling regressions.
3. Confirm full compiler, normal/slow lint, architecture, transitive axiom and
   inventory checks at the accepted source SHA; only then update coverage.

M04 remains open. No M03 evidence or original coverage obligation is removed.
